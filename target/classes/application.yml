spring:
  application:
    name: base-service
  profiles:
    active: 'dev'

server:
  port: 8000
  tomcat:
    connection-timeout: 36000
  servlet:
    context-path: /base

springfox:
  documentation:
    swagger-ui:
      enabled: true

